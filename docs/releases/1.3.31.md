# Release 1.3.31+251 - September 23, 2026

Android videos in My Media open directly in the native player without an
intermediate dialog. Approved videos now have bounded, cached thumbnails on
Android and web. The release also includes compatible playback for affected
Android decoders, recording lifecycle recovery, reuse of the existing Android
task when sharing media, and video moderation based on verified frame results.

- Android package: `com.betshuva.app`
- Version name: `1.3.31`; version code: `251`
- In-app version and fallback download link: `1.3.31`
- Web build ID (UTC): `20260922214856`
- APK size: `142409582` bytes
- APK SHA-256: `a4497c007c2bf688536f057359772e7b078598a4cb186c2f3f582e5048ca9192`
- Signing certificate SHA-256: `840a648043c790554bcdb089bdc0fdc70b1c4585ec5f737b788646131e3a76c7`; matches the previous release
- Published JavaScript SHA-256: `14d3de073596b022f03b6630b13ea9f281198da67ea638df80899e54de5e1b48`

Validation: 451 Flutter VM tests, 2 browser thumbnail tests, 11 standalone
Kotlin HTTP video source scenarios, and 2 web deployment tests passed. Backend
tests: 753 passed, 58 optional database/profile-asset checks skipped, 0 failed.
Targeted analysis of 12 media source/test files found no issues. Android and
web release builds succeeded. The universal APK contains arm64-v8a,
armeabi-v7a and x86_64 code, is not debuggable or test-only, and its signature
and packaged version were verified.

The live [version API](https://betshuva.com/betshuva-app/api/version) returns
HTTP 200 and advertises `1.3.31` with the versioned APK URL.
[Version metadata](https://betshuva.com/betshuva-app/version.json) reports build
`251`. Complete downloads from both the
[versioned APK](https://betshuva.com/betshuva-app/betshuva-1.3.31.apk) and
[latest APK](https://betshuva.com/betshuva-app/app-release.apk) returned HTTP 200
and matched the published size and SHA-256. Public index, bootstrap and
JavaScript bytes match the deployed web build and use the same cache key.

Anonymous Playwright checks at 1440x1000 and 393x852 rendered the live app,
displayed version `1.3.31`, and confirmed that clicking the download button
targets the new versioned APK. Both bootstrap and JavaScript requests used
`20260922214856`; no page exceptions, blank screen or horizontal overflow were
observed. An existing, nonblocking Google sign-in logo request is CORS-blocked
by its third-party host; the sign-in text button remains visible.

No physical-device installation of this release APK was performed during
publication. Unrelated legal/company documents and historical untracked APKs
were preserved and excluded from the release commit.
