# Personal calendar

The fixed “לוח שנה” conversation entry opens a private calendar in the desktop detail pane or a full-screen route on mobile. It initially shows the current week with today selected, using the calendar's configured time zone. Day/week timelines and a month grid support timed and all-day events, overlapping appointments, colors, notes, locations, and finite recurring series (up to 104 occurrences). Recurrence supports every 1–7 days, selected weekdays, or the same Gregorian date each month (the last day in a shorter month). Users choose a total occurrence count or an inclusive final date; a date range exceeding 104 occurrences is rejected without creating a partial series. The editor summarizes the schedule, including the actual first matching weekday.

Editing defaults to the selected occurrence, with an explicit option to update all noncancelled occurrences in the series, including past dates. Changing its schedule shifts other occurrences by the same local-calendar offset and applies the selected duration. Metadata-only edits preserve individually customized schedules. Shared fields and invitees apply to all selected occurrences; rescheduled invitations need a new response, while unchanged schedules retain their responses. Cancelling remains an individual-occurrence action. Series edits are atomic, supported for existing series, and checked against an owner-only revision covering every active occurrence, including dates outside the visible range. A stale edit requests a refresh instead of overwriting a concurrent change.

Friends can be invited from the owner's contacts. Pending invitations are separate from the calendar; accepting or replying “maybe” adds the event to the invitee's view. Only the owner can edit, cancel, or inspect the full attendee list. Time changes require a new response. Updates and reminders are persisted in the calendar inbox and also sent through the existing push service, subject to device notification permission. Bulk updates send one update notice per affected invitee. Reminder claiming prevents duplicate inbox entries across worker ticks; a separate reminder revision also preserves that protection across metadata-only edits. The additive schema migration initializes existing reminder revisions from event versions. Calendar data belongs to the account and is removed by account/data deletion.

Calendar location defaults to an explicit saved calendar choice, then the profile city, then available automatic location, and finally Jerusalem. Defaults display holidays immediately. The location dialog uses the same searchable locality picker as profile/listing forms, without editable coordinates. A searchable IANA time-zone list allows a separate choice; changing a city fills its zone and Israel/Diaspora schedule, while preserving explicit overrides made before saving a typed city. Candle lighting uses a fixed 15-minute advance for every city. The server enforces this for existing saved preferences, location defaults and older clients; the preference field is removed. After-nightfall candle lighting between consecutive holy days follows the provider's calendar rules. Hebcal's public REST API provides Hebrew dates, holidays, candle lighting and 8.5-degree nightfall. A second query adds the explicitly labelled fixed 72-minute Rabbeinu Tam calculation, including night-time candle lighting when holy days continue into each other. Responses are cached server-side for 24 hours, bounded to 128 date/location combinations. Only dates and location parameters are sent to Hebcal, never appointments or account identifiers. Failures display an availability message without suppressing personal events.

Device location is used automatically only when permission is already granted. The current-location button can request permission; unavailable location keeps the Jerusalem fallback. Automatic defaults never overwrite an explicit calendar choice. City centers and time zones are resolved locally on the server using public data (data.gov.il, GeoNames, geo-tz), without sending the user's GPS coordinates to a geocoding API. A city that cannot be resolved gets an explicit error instead of saving stale coordinates.

Events store UTC instants with their original IANA zone. Repeats preserve civil time across DST. Nonexistent and ambiguous event times are rejected with a user-facing explanation. All-day dates remain calendar dates across participant time zones. The client renders server-provided civil date/time values independently of the browser time zone.

## Validation

- Flutter analyzer: no issues.
- Calendar UI and conversation navigation tests: 12 tests covering desktop/mobile entry, day/week/month, overlapping events, creating an invitation, RSVP, and holiday-provider failures.
- Calendar server/database and account-deletion tests: 11 tests, including real PostgreSQL TEMP tables (no production rows written), access isolation, stale-version rejection, re-approval after rescheduling, cancellation, DST recurrence, all-day dates across the date line, reminder deduplication and account cascades.
- Live read-only Hebcal checks: Jerusalem and New York, Rosh Hashanah spanning Shabbat, plus Shabbat and Yom Kippur exit times.
- Broader Node suite: 534 passed, 15 skipped, 7 pre-existing failures. All seven were reproduced against HEAD versions of the application/server sources: blocked-image-without-guide-art, blocked-upload-recipient, conversation-scroll-lock (3), group-message-content-layout, self-group-web. These source-pattern checks are unrelated to the calendar.

## Deployment

`server/calendar.js` exports an idempotent additive schema applied after the existing startup migrations. No separate database credentials or external calendar connection is required. The reminder worker runs in the primary application process every 30 seconds. The existing Flutter web release process builds the client; no Android binary is included in this web release.

Source: https://www.hebcal.com/home/195/jewish-calendar-rest-api

Published web build: `20260916232130`. The calendar settings source migration is installed. Authenticated read-only checks of settings, city resolution and events returned HTTP 200; unauthenticated location access returned HTTP 401. Published index/bootstrap/application/icon-font hashes match the tested build.

Location update validation: 27 Flutter widget tests cover the shared autocomplete (including stale responses and Hebrew spelling aliases), city resolution, timezone selection with a mobile keyboard, preservation of manual overrides, permission/fallback precedence, and existing calendar navigation. The release web build and targeted Flutter analysis passed.

The location update also passed 12 server/account tests (including isolated PostgreSQL TEMP tables) and both deployment checks.

## Public location data

`server/data/calendar-locations.json` contains public city-center data from data.gov.il and GeoNames, with reviewed Hebrew aliases. Rows are `[name, latitude, longitude, countryCode, timezone, aliases, optionalGovernmentLocalityCode]`. The current snapshot resolves 1,192 of 1,285 official localities; unsupported/ambiguous names report an error. Automatic Israeli matching uses verified official locality rows. Coordinates are approximate city centers, not a user's precise stored position.

GeoNames data is adapted under [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/); source URLs, snapshot date, and alias corrections are retained in the JSON. Government ITM centers are converted to WGS84. Time-zone boundaries use the server-only geo-tz package.

To reproduce a candidate snapshot, install build-only `proj4` outside the application, run `scripts/build-calendar-location-data.py` with explicit `--source-dir` and `--output`, then run `scripts/review-calendar-location-data.py` with that source directory plus `--input` and `--output`. The review pass asserts source record identities and stops if upstream names/coordinates changed; inspect those changes before replacing the committed snapshot. Neither script processes user locations.

Fixed candle-lighting update: direct Hebcal checks with `b=15` for 25 September 2026 returned Jerusalem 18:17, Tel Aviv 18:18 and Haifa 18:17. The same fixed parameter is used for the normal and Rabbeinu Tam requests.

Fixed-15 validation: 6 server tests including isolated PostgreSQL integration, 16 Flutter widget tests and both deployment checks passed; targeted Flutter analysis is clean. Live authenticated reads for two existing accounts returned fixed-15 settings and holiday data successfully; published files match the tested build, and the removed input label is absent from the deployed application.

Hebrew navigation update: month grids and previous/next navigation now follow Hebrew months, including both Adars and the Elul/Tishrei year boundary. Toolbar and event-editor date pickers offer Hebrew month/year choices and Hebrew day numbers, with a Gregorian reference date. Date conversion uses civil calendar dates locally, so Hebrew labels and navigation remain available when the holiday provider is unavailable. Event storage and explicitly Gregorian monthly recurrences keep their existing semantics.

The selected date has a blue month-cell border, a highlighted day/week header, and a tinted time column. Selection survives view changes; selecting a week header, timeline event, or time slot selects that day for new events. Today retains a separate indication. Navigation arrows keep their physical right/left direction in RTL, and long Hebrew labels fit narrow web viewports.

Hebrew navigation validation: 56 Flutter unit/widget tests and 24 Chrome widget checks passed, including leap-month/year transitions, selection persistence, event creation, accessibility, date bounds, and 320-pixel layouts. All 109,938 Gregorian dates from 1900 through 2200 were checked against ICU Hebrew dates and conversion round trips. Targeted analysis, the release web build, and both deployment checks passed. Published web build `20260917131732`; public index/bootstrap/application/icon-font hashes match the release files, and Chromium loaded the new application without startup errors.

Event attendance update: event cards show the title and start/end time, organizer-only accepted/total invitation counts, and location when the existing card bounds allow. Short/overlapping events and month/all-day chips keep their original size; hover and accessible descriptions include the summary, response breakdown, a notes preview, and reminder. Known zero invitees display as a personal event; missing summary data remains unknown. Time ranges use explicit LTR isolation inside the Hebrew interface.

The events API adds `attendee_summary` only for organizer-owned events, using one aggregate query for all owned events in the requested range. Counts include accepted, maybe, pending, and declined invitations, and exclude the organizer even for legacy self-invitation rows. Guest responses and event visibility retain their existing rules. Opening event details loads names and responses only for the organizer, recalculates the displayed totals from the fresh attendee list, and offers retry on failure. Invitees see their own response only. Details include the configured reminder, location, and full notes. This change requires no schema migration.

Attendance validation: 34 Flutter tests and 12 Chrome widget tests passed, along with targeted analysis and 7 server tests including isolated PostgreSQL TEMP-table integration (none skipped). Server coverage checks all response states, privacy, organizer exclusion, batched reads, rescheduling, removal, recurrence, and cancellation. The restarted application returned HTTP 200 for the public version and HTTP 401 for anonymous calendar access. An authenticated read-only check confirmed the live owner summary matches the database and the attendee list excludes the organizer.

Attendance release: web build `20260917134604` is published. Both deployment checks passed, public index/bootstrap/application/icon-font hashes match the release, and Chromium loaded the new application without startup errors. The application service remains active after its required restart.

Current-week default release: web build `20260917135426` opens the current week with today selected. The calendar behind an input dialog keeps its height while the dialog handles the keyboard. Validation passed: 25 calendar tests, both targeted Chrome checks (initial week/date range and mobile keyboard), targeted analysis, release build, and both deployment checks. Public artifact hashes match the release and Chromium startup passed.

Recurrence and series-edit validation: 20 Flutter widget tests, targeted Flutter analysis, 14 server tests (including isolated PostgreSQL integration and legacy reminder migration) and both deployment checks passed. Tests cover selected weekdays, intervals, inclusive termination, count limits, short-month overnight/all-day durations, DST validation, atomic bulk updates, invitation responses, cancelled/other-series isolation, stale peer revisions outside the visible date range, customized schedules and reminder deduplication. Web build `20260917195913` was published after confirming source files stayed unchanged during compilation; live web files match the build hashes. The restarted service, migrated reminder column, authenticated calendar GET and anonymous-access rejection were verified without creating live test events.
